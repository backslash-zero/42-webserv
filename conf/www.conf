server {
    listen      0.0.0.0:8080;
    root        ./www;
    index       index.html;
    server_name webserv;

    location / {
        methods GET HEAD OPTIONS;
    }

    location /test {
        methods GET HEAD;
		root		./www/test;
		index		index.html;
    }
}
